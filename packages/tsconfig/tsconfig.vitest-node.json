{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.node.json",
  "compilerOptions": {
    // Node-specific values are derived from the node config
    "types": ["node", "vitest/globals"]
  }
}
