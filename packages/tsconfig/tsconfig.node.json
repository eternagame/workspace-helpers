{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // The vitest-node and cypress configs override types, so make sure any changes made here are reflected there
    "types": ["node"]
  }
}
