{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // The jest-node and cypress configs override types, so make sure any changes made here are reflected there
    "types": ["node"]
  }
}
