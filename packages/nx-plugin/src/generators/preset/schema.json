{
  "$schema": "http://json-schema.org/schema",
  "$id": "preset",
  "cli": "nx",
  "title": "Initializes an Nx project",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the application",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the name of the root package?"
    },
    "description": {
      "type": "string",
      "description": "Project description",
      "x-prompt": "Provide a brief description for the project",
      "pattern": "^[\\w -_']*$",
      "default": ""
    },
    "npmScope": {
      "type": "string",
      "description": "Npm scope packages should be scoped to/published under",
      "default": ""
    },
    "release": {
      "type": "string",
      "description": "Release configuration to use",
      "enum": ["no-release", "no-publish", "default-private", "default-publish"],
      "x-prompt": {
        "message": "How do you want to set up releases?",
        "type": "list",
        "items": [
          { "value": "no-release", "label": "Don't configure a release workflow" },
          { "value": "no-publish", "label": "Don't configure publishing as part of the release workflow" },
          { "value": "default-private", "label": "Configure publishing, but assume packages are private (not published) by default" },
          { "value": "default-publish", "label": "Configure publishing and assume packages should be published by default" }
        ]
      }
    },
    "license": {
      "type": "string",
      "description": "Repository license",
      "enum": ["MIT", "BSD3", "Custom", "None"],
      "x-prompt": {
        "message": "Which license would you like to use?",
        "type": "list",
        "items": [
          { "value": "MIT", "label": "MIT" },
          { "value": "BSD3", "label": "BSD 3-Clause" },
          { "value": "EternaNoncommercial", "label": "Eterna Noncommercial" },
          {
            "value": "Custom",
            "label": "Include no default license, but specify SEE LICENSE IN LICENSE in the package.json"
          },
          {
            "value": "None",
            "label": "Include no default license (you may add a LICENSE file and modify the package.json license field yourself)"
          }
        ]
      }
    },
    "copyrightHolder": {
      "type": "string",
      "description": "Who should be designated as holding the copyright in the LICENSE file",
      "x-prompt": "If you've specified a license, who is the copyright holder?",
      "default": ""
    },
    "readmePrologue": {
      "type": "string",
      "description": "Additional information to add as part of the high-level description in the README",
      "default": ""
    },
    "eternaDefaults": {
      "type": "boolean",
      "description": "Use defaults intended for packages manged by the Eterna project",
      "default": false
    }
  },
  "required": ["description"]
}
